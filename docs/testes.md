# Testes

O backend usa [Pest](https://pestphp.com/) (`pestphp/pest`), configurado em `backend/phpunit.xml` com as suites `Unit` e `Feature` (`backend/tests/Unit`, `backend/tests/Feature`).

```bash
./vendor/bin/sail artisan test
# ou
composer test
```
